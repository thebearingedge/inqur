dist: trusty
language: node_js
node_js: 8
env:
  - CXX=g++
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++
  postgresql: 9.6
services:
  - postgresql
before_script:
  - psql -U postgres -c 'create database inqur;'
  - export PGDATABASE=inqur
  - npm run db:up
script:
  - npm run cover
